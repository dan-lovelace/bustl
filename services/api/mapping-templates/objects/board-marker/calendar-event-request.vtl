{
  "version": "2017-02-28",
  "operation": "BatchInvoke",
  "payload": {
    "context": $util.toJson($context),
    "operation": "listOne",
    "gqlArgs": {
      #if($context.source.calendar_event_id)
      "id": "$util.toJson($context.source.calendar_event_id)",
      #end
    }
  }
}
